# AT03 P4 — observed failure learning

The structured record is FAILURE-TAXONOMY.json. Each case contains observation, evidence, classification, consequence, minimum discriminating next check and resolution state.

Confirmed: Vela delivery shape (F7), Janus repository discovery/count contradictions (F1/F5), a Vela command lookup failure (F4), missing review-stage context and reviewer confusion (F2/F6), and an incomplete early precheck in the task-specific verifier (F6). Earlier manual-transcription failures (F3/F5/F6) are inherited, resolved evidence lessons and are not rerun.

No runtime concurrency corruption (F8), capability mismatch (F9), or blocking frozen Foundation defect (F10) was demonstrated. Empty categories are intentional.

Limitations are not automatically failures: Janus's reused context cannot prove fresh-owner resume; Seshat's unknown project association and one-index coverage are honest boundaries. The schema hypotheses remain candidates. The project has not demonstrated general autonomous research or reliable broader census.

Decision: one small fresh-owner work loop is safe to attempt using the already accepted frozen Foundation manifest, without integrating held pilot records. The loop gets one execution/verification attempt; failure will be preserved, not repeatedly repaired into green. Foundation v0.1 remains unchanged.
