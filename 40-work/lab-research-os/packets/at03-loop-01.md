# AT03-loop-01 — one bounded autonomous work attempt

Authorization: AT03 P3/P4 assessment is closed. Astra delegates this single low-risk loop, including the final project checkpoint, to a fresh-context owner. Machine packet at03-loop-01.json supplies paths, baseline, permissions, exclusions and budgets. This is not a permanent agent or scientific module.

## Restore before execution
Use existing resume/handoff procedure. Discover marker from a child directory, load AGENTS/PROJECT, inspect committed state through foundation.py/current, read relevant P3/P4 decisions and this packet. Record actual loaded/necessary/missing/excess context. Do not use conversation reconstruction. Expected checkpoint is the packet value; if newer state invalidates the task or write ownership, record F8 and stop. Coordinator will not write while you own this attempt.

## Task and source
Only task data source: evidence/foundation-v0.1-manifest.json, already accepted as the frozen release snapshot. Create an index of that manifest, not a new audit of actual installed files. Do not rerun 8/10 tests or recompute every frozen file's hash. Do not consume or integrate held pilot inventories.

Candidate index exact keys: project_id, release, manifest_path, manifest_sha256, entry_count, entries. project_id comes from the marker; release from manifest; manifest_path is the canonical absolute source path; manifest_sha256 hashes its actual bytes; entry_count equals len(files); entries is the manifest files array sorted by path, with each item exactly {path,sha256}. No invented metadata or live-state claim. Produce candidate by program, never manually transcribe hashes.

## One attempt procedure
1. Capture primary source bytes/hash and context receipt in your write set. Record UTC times and actual command streams automatically. Keep observation separate from validator outcomes and model review.
2. Generate index.candidate.json. Deterministic checks compare actual source/index fields, counts, sorted entries, duplicate paths, hash syntax and path containment. Capture comparisons and exact command outcomes. A test fixture or hard-coded expected PASS is not approval. Do not certify validator correctness or scientific truth.
3. Make exactly ONE independent DeepSeek Flash evidence-package review using the existing DSH text route. Existing entry is evidence/at02-p0-deepseek-text.patch.yml and its runner. Use child-process-only LAB_TEXT_INPUT/LAB_TEXT_OUTPUT/LAB_TEXT_MODEL=deepseek-flash; invoke the already installed node and DSH CLI paths as shown in evidence/at02-p0-exact-review.py. Reuse entry points, do not run that old review script. No tools for the reviewing model, no credentials/config reads/edits.
4. Assemble the review prompt directly from exact source manifest, candidate, deterministic check output and relevant derivation/validation code, with byte hashes. Do not manually abbreviate primary evidence. Ask for concise JSON result(PASS/FAIL/UNCERTAIN), confidence0..1, claim, evidence, verification_method, anomaly_conflict[], limitations[]. Scope only faithful index derivation from an accepted stored manifest; not live re-audit or independent acquisition. Stage is independent review now; pending review is not an input defect. Do not ask for or assume a high score. Preserve original prompt, response, session/completion/tool-call metadata, exact execution stdout/stderr/exit/timing. Max one call; no retry on low confidence/parse/timeout/failure.
5. Apply existing >=0.80, PASS, no unresolved anomaly, completed response, zero-tool gates. If any gate fails, retain candidate and failure evidence, do NOT create the canonical index. Do not lower thresholds or call another model to make the attempt green.
6. If all gates pass, create artifacts/at03/manifest-index-v0.1.json with exclusive create, identical to the accepted candidate bytes, and capture writeback equality/hash. No overwrite of an existing artifact.
7. Checkpoint TRUE outcome immediately using the existing foundation checkpoint function/CLI and the exact expected parent. All eight continuity fields remain required. Stage=AT03-P5, session=AT03, priority=P3 (existing domain enum). If steps succeeded, commit a narrowly scoped AUTONOMOUS WORK LOOP v0.1 — PROVEN claim (manifest indexing only); the successful pointer commit completes the loop. Otherwise commit NOT_PROVEN with observed failure class/next minimal check. No repeated repair/retry of a failed loop. If checkpoint itself fails, retain failure receipt and do not claim PROVEN.
8. Capture checkpoint-commit.json and loop-outcome.json with the real result, new ID if any and evidence references. Preserve all attempted/failed files. Return concise outcome and exact resume point, not a retelling of chat history.

## Output, stops, exceptions
All intermediate files go only in artifacts/at03/autonomous-loop-01/. Suggested durable files: context-load.json, source-manifest.snapshot.json, source-binding.json, index.candidate.json, deterministic-checks.json, review-prompt.txt, review-response.json, review-execution.json, writeback-receipt.json, checkpoint-commit.json, loop-outcome.json. Use a small local runner if helpful; no generic framework.

Stop and preserve evidence on source ambiguity, unsupported identity, scope escape, missing evidence, stale parent/lock, model failure or any acceptance failure. Ordinary failure is a valid terminal loop result. No Human PI interruption is needed for a safely held local artifact. High-impact/irreversible consequence would require an exception, not action.

The model's requested routing identity must be distinguished from actual response claims. Record controller/task harness and deterministic runtime separately. One successful loop proves only this narrow handoff/writeback/checkpoint path; it does not qualify general research autonomy or wider census.
