Review the following P2 contract source excerpt and reported test evidence.

contracts.py critical gates: packet_check validates task-packet schema, canonical .lab-project.json project identity, absolute paths, and write paths inside project root. evidence_check validates evidence schema, absolute path under approved roots, file existence/size, and SHA256 bytes. bundle_check requires exactly task_id/records/evidence/reports; matching task identity; nonempty lists; evidence count within packet budget; unique IDs; schema-valid inventory records; all record/report evidence references resolved; every report result PASS, confidence >= 0.8, and no anomaly_conflict; every record evidence ID covered by a report. It returns PASS only for these structure, reference, integrity, and gate checks, not semantic truth or runtime isolation.

Reported evidence p2-test-run.json: command `python -m unittest tests.test_contracts -v`, exit_code 0, Ran 10 tests, OK, with tests covering valid bundle acceptance, project identity mismatch, write-path escape, evidence hash mismatch, approved-root/traversal rejection, malformed report rejection, missing evidence reference, FAIL/UNCERTAIN/low-confidence/open-anomaly integration holds, evidence budget, and record evidence coverage. The report also records source hashes for tests/test_contracts.py and tools/contracts.py. This is reported test evidence; do not execute tests.

Task: provide a bounded verification report with exactly these labels: Claim, Evidence, Verification Method, Result, Confidence, Anomaly/Conflict. Review of source and reported test evidence, not test execution. State that the test assertions are consistent with the described gates if supported. Include a brief mocks inventory and disclose that the tests use temporary fixtures and mock the absent P3 project schema only in the valid-bundle path; this review does not test production integration, semantic truth, runtime isolation, or independently execute the tests. If test provenance is insufficient, limit the claim to code/test-contract consistency and do not claim independently executed tests. Do not call tools, browse, or invent source facts. Do not assume model identity beyond any returned trace.
